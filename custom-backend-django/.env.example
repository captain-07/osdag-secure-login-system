SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
DB_NAME=osdag_login
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
REDIS_URL=redis://localhost:6379/1

# Origins allowed when DEBUG=False. The browser client in ../frontend is
# served on :5500, so it must be listed here when CORS_ALLOW_ALL_ORIGINS is off.
CORS_ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500